package `2 Basics`

fun main() {

    /**
     * true yada false değer atamsı için kullanılır .
     * 0 ya da 1 olarak kullanılmaz.
     */

    val isStudent: Boolean = true
    val isTeacher: Boolean = false

    /**
     * && ,|| , and ,or xor gibi operatörlerle beraber kullanılabilir.
     */

    if (isStudent && isTeacher) {

    }
    if (isStudent and isTeacher) {

    }
    if (isStudent || isTeacher) {

    }
    if (isStudent or isTeacher) {

    }
}